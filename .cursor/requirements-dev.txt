# CPU-only dependency set for Cloud Agent / headless development.
#
# The full app (app/requirements.txt) targets NVIDIA CUDA hardware and pulls
# GPU-only wheels (onnxruntime-gpu, rembg[gpu], xformers, etc.) plus tens of
# GB of model weights that Pinokio downloads lazily on real hardware. None of
# that runs in a GPU-less Cloud Agent VM.
#
# This file pins the subset that lets the repository's own checks run headless:
#   - scripts/verify_clean_repo.py        (clean-repo guard)
#   - python -m compileall app/services … (syntax check)
#   - python -m unittest discover tests   (776 tests, GPU not required)
#   - python tests/test_call_llm_json_grammar.py
#
# torch / torchvision / torchaudio are installed separately from the PyTorch
# CPU index by .cursor/install.sh (they are not published on PyPI as +cpu).
#
# Versions match app/requirements.txt where it pins them; the CI guard job's
# lightweight pins (starlette/numpy/opencv/Pillow/requests) are used as-is.

# CI guard job lightweight deps
starlette==0.46.1
numpy==2.2.6
opencv-python-headless==4.12.0.88
Pillow==11.3.0
requests==2.32.4

# Test runner + light libraries imported by the suite
pytest==8.3.5
websocket-client==1.9.0
tqdm==4.67.3
psutil==7.2.2
json_repair==0.59.5
huggingface_hub==0.36.2

# AI stack (CPU wheels; pins follow app/requirements.txt)
einops==0.8.2
safetensors==0.8.0
mmgp==3.7.6
diffusers==0.36.0
transformers==4.57.1
tokenizers==0.22.1
accelerate==1.12.0
timm==1.0.24

# Vision / media libraries the tests import
av==16.1.0
imageio==2.37.2
imageio-ffmpeg==0.6.0
decord==0.6.0
scipy==1.18.0
ftfy==6.3.1
ffmpeg-python==0.2.0
iopath==0.1.10
pycocotools==2.0.11

# rembg CPU variant (app/requirements.txt uses rembg[gpu]); onnxruntime is the
# CPU counterpart to the GPU build's onnxruntime-gpu.
rembg==2.0.65
onnxruntime==1.28.0
